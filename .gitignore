# ============================================================================
# 教材内容 —— 绝对不要提交
# ----------------------------------------------------------------------------
# 这个仓库只放「工具」。教材 PDF、扫描页、以及从课本词表抽取出来的释义都是
# 受版权保护的内容或其衍生作品，公开仓库等于向公众传播。
# 使用者应当自备合法获得的 PDF，然后自己跑 tools/run_all.py 生成这些文件。
# ============================================================================

# 原始教材
textbook.pdf
*.pdf
!docs/**/*.pdf

# 扫描页（从 PDF 抽出 / 缩放得到）
pages_raw/
pages/
dist/

# 从课本抽取的内容（衍生作品）
data/pages.json
data/pages_ocr.json
data/ocr_all.json
data/ocr_for_correction.json
data/vocab_raw.json
data/lex_glossary.json
data/dict.json

# 生成物（可随时由 run_all.py 重建）
data/lex_verbs.json
data/lemmas.json
data/dict_stats.json

# 诊断输出
data/_*.txt
data/_*.log
cache/

# ---------------------------------------------------------------------------
# 下面这些是「工具自带、可公开」的数据，故意不忽略：
#   data/dict_seed.json      手写的 1569 个常用西语词
#   data/dict_extra.json     手写的缩合词/物主形容词
#   data/lex_irregular.json  通用西语不规则动词变位
# ---------------------------------------------------------------------------

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 编辑器 / 系统
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
